#! /bin/sh
#

# if there is a reason not to shutdown, tell VDR about it
REASON="$(check-lifeguard | sed -n '/boolean false/ {n;s/string[[:space:]]*"\(.*\)"/\1/;p}')"
if [ -n "$REASON" ]; then
	echo "ABORT_MESSAGE=\"$REASON\""
	exit 1
fi
